services:
  # rreading-glasses - Metadata service for Readarr
  rreading-glasses:
    container_name: rreading-glasses
    image: blampe/rreading-glasses:hardcover
    networks:
      - t3_proxy
    security_opt:
      - no-new-privileges:true
    restart: unless-stopped
    profiles: ["all", "apps"]
    depends_on:
      postgres-rreading:
        condition: service_started
    command:
      - --verbose
    entrypoint: ["/main", "serve"]
    mem_limit: 128m
    environment:
      TZ: $TZ
      PUID: $PUID
      PGID: $PGID
      HARDCOVER_AUTH: $HARDCOVER_API_KEY
      POSTGRES_HOST: postgres-rreading
      POSTGRES_DATABASE: rreading-glasses
      POSTGRES_USER: rreading-glasses
      POSTGRES_PASSWORD: $POSTGRES_RREADING_PASSWORD
    labels:
      # Enable WUD
      - "wud.watch=true"
      - "wud.tag.include=^latest$$"
